# AirClaw
#
# 【重要】后端依赖的唯一真相源是 backend/requirements.txt（pip 安装），
# 以便内网部署时用 `pip download` 预取 wheel 后在隔离环境离线安装。
#
# 本文件仅用于 uv 工具链：约束解释器版本、记录项目元数据、承载开发工具配置。
# 请勿在此处添加运行时依赖，避免出现两份互相漂移的依赖清单。

[project]
name = "airclaw"
version = "0.1.0"
description = "本地科研办公提效 Agent —— 文件即记忆，技能即插件，全程可审计"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
